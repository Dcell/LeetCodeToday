name: publish

on:
  schedule:
    - cron: '0 12 * * *'
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@master
      with:
        ref: master
    - name: binsh
      echo '1'
    - name: build
      uses: github-actions-x/hugo@master
    - name: push
      uses: github-actions-x/commit@v2.6
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        push-branch: 'master'
        commit-message: 'arr you code leetcode today?'
        force-add: 'true'
        name: Dcell
        email: 765736477@qq.com 
